const DeleteAlbumForm = ({ rowObject, backendURL, refreshAlbums }) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Delete
                </button>
            </form>
        </td>

    );
};

export default DeleteAlbumForm;